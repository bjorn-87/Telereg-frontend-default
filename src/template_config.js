"use strict";

// Add url then change the name of the file to config.js
// important to have '/' at the end of the URL

const config = {
    baseUrl: "url for API for example: https://www.examplewebsite.com/",
};

export default config;
